<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Bucket List del 18", root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item ml-auto">
          <%= link_to "Salir", destroy_user_session_path, method: :delete, class: 'nav-link' %>
        </li>
        <!-- el admin puede ver el listado de productos vendidos -->
      <% else %>
        <li class="nav-item">
          <%= link_to "Acceder", new_user_session_path, class: 'nav-link' %>
        </li>
      <% end %>

    </ul>
  </div>
</nav>
